rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEMO MODE: Allow public read/write access to all collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
